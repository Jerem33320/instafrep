{% extends 'base.html.twig' %}

{% block title %}
    Editer le post {{ post.id }}
{% endblock %}


{% block headerContent %}
    <h1 class="display-4">Editer le post #{{ post.id }}</h1>
{%  endblock %}

{% block mainContent %}

    {% set action = path('post_edit', { 'id': post.id }) %}
    {% include 'post/form.html.twig' %}

{% endblock %}



{% block sidebarContent %}

    <a href="{{ url('posts_list') }}" class="btn btn-block btn-outline-primary">
        Revenir à la liste
    </a>


    <a href="{{ url('post_remove', { 'id' : post.id }) }}" class="btn btn-block btn-danger">
        !!! Supprimer le post !!!
    </a>

{% endblock %}

